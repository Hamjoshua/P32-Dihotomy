<Page x:Class="PalMathy.Pages.DihotomyPage"      
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PalMathy.Pages" 
      xmlns:oxy="http://oxyplot.org/wpf" 
      xmlns:viewmodels="clr-namespace:PalMathy.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DihotomyPage">
    <Page.DataContext>
        <viewmodels:MathViewModel />
    </Page.DataContext>
    <Grid Background="#FFF3F3F3">
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Top">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"></ColumnDefinition>
                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                
                <Label Grid.Row="0" Grid.Column="0" Content="f(x) = " />
                <Label Grid.Row="1" Grid.Column="0" Content="A = " />
                <Label Grid.Row="2" Grid.Column="0" Content="B = " />
                <Label Grid.Row="3" Grid.Column="0" Content="E = " />

                <TextBox Grid.Row="0" Grid.Column="1"
                    HorizontalAlignment="Left" Text="{Binding FunctionString}" Width="100px" />
                <TextBox Grid.Row="1" Grid.Column="1"
                    HorizontalAlignment="Left" Text="{Binding A}" Width="100px" />
                <TextBox Grid.Row="2" Grid.Column="1"
                    HorizontalAlignment="Left" Text="{Binding B}" Width="100px" />
                <TextBox Grid.Row="3" Grid.Column="1"
                    HorizontalAlignment="Left" Text="{Binding Epsilon}" Width="100px" />
            </Grid>
            <Label Content="Функция" />
            
            
            <TextBox HorizontalAlignment="Left" Text="{Binding BeginInterval}" Width="50px"></TextBox>
            <TextBox HorizontalAlignment="Right" Text="{Binding EndInterval}" Width="50px"></TextBox>
            <Button Content="Построить график" Command="{Binding CalculateFunction}" VerticalAlignment="Top" Width="Auto"/>
            <Button Content="Посчитать корни" Command="{Binding CalculateResult}" VerticalAlignment="Top" Width="Auto"/>

            <TextBlock Text="{Binding Result}"></TextBlock>
        </StackPanel>

        <oxy:PlotView Grid.Column="1" x:Name="pvGraph" Model="{Binding Plot}"/>

    </Grid>
</Page>
